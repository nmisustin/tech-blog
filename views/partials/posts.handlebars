<article class="post">
    <div class="title ">
        <h2><a class="btn btn-secondary" href="/post/{{id}}">{{title}}</a></h2>
        <p class="meta">Posted by {{user.username}} on {{created_at}}</p>
    </div>
    <div class="content">
        
        {{post_content}}
    </div>
</article>